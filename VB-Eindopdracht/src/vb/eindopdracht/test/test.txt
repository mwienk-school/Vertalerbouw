const aPill = blue; 
const thePill = red;
spawn powerPill;
spawn ohnoyoudInt;
spawn ichooseyouChar;
{ spawn powerPill; 
  read(powerPill); };
read(ohnoyoudInt, powerPill);
print(6*2);



spawn var1Int;
spawn var2Int;
spawn varPill;
spawn var1Char;
spawn var2Char;
const const1Int = 1;
const const2Int = 2;
const constPill = red;
const constChar = 'b';

print(var1Int);
print(var2Int);
print(varPill);
print(var1Char);
print(var2Char);
print(const1Int);
print(const2Int);
print(constPill);
print(constChar);

+16 + 2 * -8;

12 / 5 * 5 + 12 % 5 == 12 && 6 >= 6;
var1Int < var2Int && const1Int <= const2Int;
const1Int * const2Int > var2Int - var1Int;
var1Char == constChar && (var2Char != 'b' || !red);
!blue && varPill = constPill || red != blue;

'b';

var2Int = var1Int = +16 + 2 * -8;
varPill = 12 / 5 * 5 + 12 % 5 == 12 && 6 >= 6;
var2Char = 'z';

read(var1Int, var2Int);
read(varPill);
read(var1Char);

write(var2Int);
write(varPill);
write(!blue && varPill == constPill || red != blue);
write(var1Int, var2Int);
write(var1Int < var2Int && const1Int <= const2Int, const1Int * const2Int > var2Int - var1Int);
write('c');
write('a', var1Char = constChar && (var2Char != 'b' || !red));
